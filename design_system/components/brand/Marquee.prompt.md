`Marquee` runs disciplines or client names between two hairlines. Use it once per page, usually directly under the hero.

```jsx
<Marquee items={["Brand identity","Art direction","Web design","Type"]} speed={32} />
```
