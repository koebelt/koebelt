`SectionHeading` opens every section on the site. The hairline above it is part of the component — don't add another.

```jsx
<SectionHeading eyebrow="01 — Work" title="Selected projects" description="Six of thirty."
  action={<Button variant="ghost">All work</Button>} />
```
