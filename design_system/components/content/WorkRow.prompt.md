`WorkRow` is the list alternative to `ProjectCard` — use it for the full index where the grid would run too long.

```jsx
<WorkRow index="01" title="Atelier Vasse" discipline="Brand identity" year="2025" />
```
