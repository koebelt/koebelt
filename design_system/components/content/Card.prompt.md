`Card` is a flat raised panel — border, not shadow. KOEBELT never stacks shadows; depth comes from a one-step background change.

```jsx
<Card padding="var(--space-8)"><h3>Art direction</h3><p>…</p></Card>
```
