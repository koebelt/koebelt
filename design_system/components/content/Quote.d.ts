/** Pull quote with a 2px lime rule on the left. One per page, maximum. */
export interface QuoteProps {
  children?: React.ReactNode;
  attribution?: string;
  role?: string;
}
export declare function Quote(props: QuoteProps): JSX.Element;
