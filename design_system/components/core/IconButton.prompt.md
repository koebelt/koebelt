`IconButton` is a square 32/40/48px control holding a single Lucide glyph. Always pass `label`.

```jsx
<IconButton name="github" label="GitHub" />
<IconButton name="x" label="Close" variant="bare" size="sm" />
```
