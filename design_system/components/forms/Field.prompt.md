`Field` supplies the label, hint and error slot around any control. Labels are uppercase mono, hints are 14px body.

```jsx
<Field label="Email" htmlFor="email" required hint="I reply within two days.">
  <Input id="email" type="email" placeholder="you@studio.com" />
</Field>
```
