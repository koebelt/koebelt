`Select` for a short closed list — budget band, project type. Longer lists belong in a filter row, not a dropdown.

```jsx
<Select options={["Brand identity","Web design","Art direction"]} defaultValue="Web design" />
```
