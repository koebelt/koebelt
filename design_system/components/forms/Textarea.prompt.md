`Textarea` for anything longer than a line — the contact form's message box. Vertical resize only.

```jsx
<Field label="Project" htmlFor="p"><Textarea id="p" rows={6} placeholder="What are you building?" /></Field>
```
