`Input` is the standard text field — 48px, `--surface-sunken` fill, hairline border that turns lime on focus. Wrap in `Field` for a label.

```jsx
<Input placeholder="you@studio.com" type="email" />
```
