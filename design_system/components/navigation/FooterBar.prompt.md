`FooterBar` closes every page. The email address is the largest type in the footer — it is the call to action.

```jsx
<FooterBar email="hello@koebelt.com" socials={[{name:"github"},{name:"linkedin"},{name:"instagram"}]} />
```
